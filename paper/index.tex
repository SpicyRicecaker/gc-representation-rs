% Font size, paper type
\documentclass[12pt]{article}
% Aesthetic margins
\usepackage[margin=1in]{geometry}
% Core math packages,
% Mathtools loads amsmath, and amsmath gives basic math symbs
% mathtools includes amsmath, which includes amstext. 
% amssymb includes amsfonts. Both include misc. symbols you might need
\usepackage{mathtools, amssymb}
% file urls, hyphens option allows word-break of urls
\usepackage[hyphens]{url}
% Allows the use of \href{}{} to create hyperlinks
\usepackage{hyperref}
% Allows the use of Hhx values for colors and custom colors
\usepackage{xcolor}
% Extends the graphics package, which allows the creation of pictures, graphs, and figures
\usepackage{graphicx}
% Creates headers and footers
\usepackage{fancyhdr}
% Allows the underlining of words with line breaks normalem to disable the
% option of changing italicized text to underlined
\usepackage[normalem]{ulem}
% Adjust accordingly given warning messages
\setlength{\headheight}{15pt}
% Allows us to specify where a picture should be placed with respect to words 
\usepackage{float}
% Allows the user to make custom images and graphs inside of latex
\usepackage{tikz}
% Allows the use of custom fonts
% \usepackage[T1]{fontenc}
% UNEEDED ~~Sets the encoding of the file to unicode instead of ascii~~ luatex and xetex default to unicode
% \usepackage[utf8]{inputenc}
% Set the font to arial
% \usepackage{fontspec}
% \usepackage{mathspec}
% \setmainfont{Arial}
% \setmathrm{Arial}
% \setmathfont(Digits,Latin){Arial}
% Allows the creation of tables in a more procedural way 
\usepackage{tabularx}
% Allows the creation of dummy text. Similar to `lorem*[n]` in emmet
\usepackage{lipsum}
% Spits columns into two, the sane way
\usepackage{paracol}
% Layout of two columns
\usepackage{multicol}
% Removes automatic indent at the start of a paragraph
\usepackage{indentfirst} 
% Allows us to split a latex file into multiple files. Modularity! Includes
% other .tex files, precompiled Use \subfile{path_of_doc} to include other files
\usepackage{subfiles}

% Allows custom specification of in-tex citations.
% Uses `biber', an external program, as a backend!
\bibliographystyle{plain}
\usepackage[backend=biber,dateabbrev=false]{biblatex}
\addbibresource{./citations.bib}

% Allows the creation of syntax-highlighted, formatted code blocks
\usepackage[outputdir=out]{minted}
\setminted{fontsize=\small}

% Sets footer style
\pagestyle{fancy}
% Removes the default footer style
\fancyhf{}

% Specifies the right header's content's
\rhead{\thepage{}}

% Makes hyperlinks look more appealing
\hypersetup{ 
colorlinks=true,
% linkcolor=blue,
citecolor=blue,
% filecolor=magenta,
urlcolor=blue,
}

\begin{document}
\subfile{body}
\end{document}